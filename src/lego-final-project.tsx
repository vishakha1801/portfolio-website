import { createRoot } from "react-dom/client";
import LegoFinalProjectPage from "@/pages/LegoFinalProjectPage";
import "./index.css";

createRoot(document.getElementById("root")!).render(<LegoFinalProjectPage />);
